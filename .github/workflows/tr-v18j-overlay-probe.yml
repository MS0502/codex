name: Probe v18j overlay run

on:
  push:
    branches: [tr-wine-compat-proton11-runtime-closure-v18i]
    paths: [.github/workflows/tr-v18j-overlay-probe.yml]

permissions:
  actions: read
  issues: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Record latest overlay runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          RUNS=$(gh api \
            "repos/${GITHUB_REPOSITORY}/actions/workflows/tr-winlator-v18j-apk-overlay.yml/runs?branch=tr-wine-compat-proton11-runtime-closure-v18i&per_page=5" \
            --jq '[.workflow_runs[] | {id,run_number,event,status,conclusion,head_sha,created_at,updated_at,html_url}]')
          BODY=$(printf 'v18j overlay run probe\n\n```json\n%s\n```\n' "$RUNS")
          gh api repos/${GITHUB_REPOSITORY}/issues/18/comments -f body="$BODY"
