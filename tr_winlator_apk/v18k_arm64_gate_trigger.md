# v18K ARM64 runtime gate

This marker triggers the base-branch pull-request workflow after its registration.
No APK or runtime binary is changed.

Attempt 9 runs v18J baseline and the two-file XShape-restored candidate in parallel on identical Xorg servers with SHAPE explicitly disabled.
